/*values that aren’t
already of an expected type are often coerced to that type. The if
statement expects a boolean, but if you pass it something that’s not
already boolean, coercion will occur.*/

// Falsy values in JS = 0 or ""

// Truthy values in JS = 99 or "free"

// Loops Definition - Repeating a set of actions until a certain condition fails—in other
// words, repeating only while the condition holds

// Iteration - Each time the loop block executes, that’s called an iteration.

